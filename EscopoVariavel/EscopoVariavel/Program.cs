using System;

namespace Escopo
{
    class Program
    {
        static void Main(string[] args)
        {
            // podemos declarar uma variavel e depois usar
            int x;

            x = 10;

            // ou podemos declarar uma variavel e na mesma linha iniciar
            int y = 10;

            // nao podemos iniciar uma variavel, nao atribuir e executar essa variavel
            
            /* Nao pode:
             * int x;
             * Console.WriteLine(x);
             * 
             */
        }
    }
}